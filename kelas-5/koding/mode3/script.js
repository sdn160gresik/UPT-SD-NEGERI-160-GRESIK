document.addEventListener("DOMContentLoaded",loadBook);async function loadBook(){const b=document.getElementById("book");let i=1,c=0;while(true){let f=`pages/page${String(i).padStart(3,"0")}.html`;try{let r=await fetch(f);if(!r.ok)break;b.insertAdjacentHTML("beforeend",await r.text());c++;i++;}catch{break}}const p=[...document.querySelectorAll(".page")];let cur=0;function s(){p.forEach((e,j)=>e.classList.toggle("active",j===cur));pageIndicator.textContent=`${p.length?cur+1:0} / ${p.length}`;prevBtn.disabled=cur==0;nextBtn.disabled=cur>=p.length-1}prevBtn.onclick=()=>{if(cur>0){cur--;s()}};nextBtn.onclick=()=>{if(cur<p.length-1){cur++;s()}};s();}